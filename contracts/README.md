# Smart Contracts (Arc)

Stub directory. Implemented in Sprint 2 by **Claude Opus 4.6**.

Planned contracts:
- `ReasoningRegistry.sol` — `mapping(bytes32 => TraceMetadata)` + events
- `RosettaToken.sol` — ERC-20 backed by USDC, OpenZeppelin base
- `PredictionMarket.sol` — binary market, USDC settlement
- `PerformanceBond.sol` — slash-bonded staking

Toolchain: **Foundry** (preferred) — `forge init` will run here.

See [`../AGENTS.md`](../AGENTS.md) §6 for security non-negotiables.
