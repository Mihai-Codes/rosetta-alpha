# Fix peer dependency conflicts between next@16, @playwright/test@1.48.2, and @synthetixio/synpress
legacy-peer-deps=true

# Note: PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD is set as a Vercel env var (not npmrc — npm 10+ rejects unknown keys)
